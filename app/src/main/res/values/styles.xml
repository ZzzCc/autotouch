<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_back</item>

        <!-- qmui theme -->
        <item name="qmui_config_color_blue">@color/app_color_1</item>
        <item name="qmui_round_btn_bg_color">@color/s_btn_app_color_1</item>
        <item name="qmui_round_btn_border_color">@color/s_btn_app_color_1</item>
        <item name="qmui_round_btn_text_color">@color/light_black1</item>

        <!-- app theme -->
        <item name="app_primary_color">?attr/qmui_config_color_blue</item>
        <item name="app_content_bg_color">@color/qmui_config_color_white</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation">
        <!-- A 打开 B，B 的出现动画 -->
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_left</item>
        <!-- A 打开 B，A 的消失动画 -->
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <!-- B 关闭到 A，A 的出现动画 *时长要小于 B 的消失动画，否则会闪烁* -->
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_right</item>
        <!-- B 关闭到 A，B 的消失动画 *时长要小于 B 的消失动画，否则会闪烁* -->
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <style name="AnimationActivity1" parent="@android:style/Animation">
        <!-- A 打开 B，B 的出现动画 -->
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_left</item>
        <!-- A 打开 B，A 的消失动画 -->
        <item name="android:activityOpenExitAnimation">@anim/slide_out_right</item>
        <!-- B 关闭到 A，A 的出现动画 *时长要小于 B 的消失动画，否则会闪烁* -->
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_right</item>
        <!-- B 关闭到 A，B 的消失动画 *时长要小于 B 的消失动画，否则会闪烁* -->
        <item name="android:activityCloseExitAnimation">@anim/slide_out_left</item>
    </style>

    <style name="AnimationFade" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>

    <style name="ToolbarCustomStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/text_color_1</item>
    </style>

    <style name="CircularProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TransparentDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/bg_black_alpha_50</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="BottomDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>
